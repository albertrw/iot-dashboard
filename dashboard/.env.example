# Optional: override API base (default is same-origin in production)
# VITE_API_BASE_URL=https://iot.bakamelabs.com

# Optional: override WS URL (default is /ws on same host in production)
# VITE_WS_URL=wss://iot.bakamelabs.com/ws

